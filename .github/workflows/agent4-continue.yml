name: Agent 4 continuation helper

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  idle:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Agent 4 continuation helper is paused between implementation stages.'
